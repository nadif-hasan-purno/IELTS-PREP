services:
  - type: web
    name: ielts-prep-app
    env: node
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
